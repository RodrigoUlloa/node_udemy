<%- include('../includes/head.ejs') %>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
          <% if (products.length > 0) { %>
            <ul>
              <% products.forEach(p => { %>
                <p><%= p.productData.title %> (<%= p.qty %>)</p>
              <% }) %>
            </ul>
          <% } else { %>
            <h1>No products in cart po ajilao!</h1>
          <% } %>
        </main>
        <%- include('../includes/end.ejs') %>